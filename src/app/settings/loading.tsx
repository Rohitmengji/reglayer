/**
 * RegLayer — Settings Loading Skeleton
 *
 * WHY: Settings page may load slowly (fetching user prefs, team data).
 * WHAT: Shimmer skeleton matching settings page layout to reduce perceived load time.
 * HOW: Next.js shows this automatically while settings/page.tsx suspends.
 */
export { default } from "../scans/loading";
